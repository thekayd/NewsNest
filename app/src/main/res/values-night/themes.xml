<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Dark theme customization -->
    <style name="Base.Theme.NestNews" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:colorPrimary">@color/DMblue</item> <!-- Dark mode blue -->
        <item name="android:colorPrimaryDark">@color/DMblue</item>
        <item name="android:colorAccent">@color/DMblue</item> <!-- Accent color -->
        <item name="android:colorBackground">@color/DMBackground</item> <!-- Dark background -->
        <item name="android:textColorPrimary">@color/white</item> <!-- White text -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/DMblue</item>
    </style>
</resources>
